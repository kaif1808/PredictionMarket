{
  "name": "predictionmarket-deployment",
  "version": "1.0.0",
  "description": "Root package file to trigger Heroku Node.js buildpack",
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build"
  }
}